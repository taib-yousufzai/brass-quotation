rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Allow unrestricted read/write access to quotations collection
    // This is for development/testing - consider adding authentication in production
    match /quotations/{document=**} {
      allow read, write: if true;
    }
  }
}