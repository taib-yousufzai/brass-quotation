rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Allow read/write to quotations collection
    match /quotations/{quotationId} {
      allow read, write: if true;
    }
  }
}